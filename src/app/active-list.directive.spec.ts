import { ActiveListDirective } from './active-list.directive';

describe('ActiveListDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveListDirective();
    expect(directive).toBeTruthy();
  });
});
